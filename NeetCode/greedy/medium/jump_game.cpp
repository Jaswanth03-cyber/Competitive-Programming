#include<bits/stdc++.h>
using namespace std ;


class Solution {
public:
    bool canJump(vector<int>& nums) { 
        bool ans = false ;
        int i = 0 ;
        int jump = 0 ;
        while(i < nums.size()){
        } 
    }
};

int main(){

    ios_base::sync_with_stdio(false) ;
    cin.tie(NULL) ;

    return 0 ;
}